- the config are just examples, change what you want and need.

- the server key is just and example. its not active anymore.

- can use npm install instead of npm install google-cloud because of package.json file.
